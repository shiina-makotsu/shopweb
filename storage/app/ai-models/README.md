Local AI model directory.

Put language models under `language/`, image generation checkpoints under `image/`,
and image LoRA files under `lora/`.

This repository intentionally does not ship model files. Configure a local runner
with `LOCAL_AI_RUNNER_URL` to actually execute these models.
